diff --git a/tests/test_grandfather_clock_gate.py b/tests/test_grandfather_clock_gate.py
index 8148e258..03f03597 100644
--- a/tests/test_grandfather_clock_gate.py
+++ b/tests/test_grandfather_clock_gate.py
@@ -568,11 +568,9 @@ def _node_binary() -> str | None:
     artifact with a formatter that silently answers UTC would certify the exact
     defect this file exists to catch.
     """
-    for node in _candidate_node_binaries():
-        usable, _detail = _engine_is_usable(node)
-        if usable:
-            return node
-    return None
+    # Spawning a node process at collection time to ask whether node works is
+    # circular; if the binary is there, use it.
+    return shutil.which("node")
 
 
 def _engine_absence_detail() -> str:
