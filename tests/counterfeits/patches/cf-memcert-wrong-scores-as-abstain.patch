diff --git a/scripts/memcert/core.py b/scripts/memcert/core.py
index 953c98e8d..980bdbd16 100644
--- a/scripts/memcert/core.py
+++ b/scripts/memcert/core.py
@@ -327,14 +327,14 @@ def grade_item(spec: AnswerSpec, raw_answer: str) -> tuple[str, float]:
             _contains_word(ans, normalize_answer(str(s))) for s in spec.stale_values
         )
         if exp_hit and stale_hit:
-            return ("wrong", -0.5)
+            return ("wrong", 0.0)
         if exp_hit:
             return ("correct", 1.0)
         if stale_hit:
             return ("stale", -1.0)
         if abstained:
             return ("abstain_miss", 0.0)
-        return ("wrong", -0.5)
+        return ("wrong", 0.0)
 
     if abstained:
         return ("abstain_miss", 0.0)
