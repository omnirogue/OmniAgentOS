# Opening Balances

For ledger operations and examples, the standard starting ledger has the following opening balances:

- `alice`: `100` (positive balance, allows withdrawals/transfers)
- `bob`: `50` (positive balance)
- `charlie`: `0` (empty balance, allowed as a target, but can't withdraw/transfer initially)

Total initial funds in ledger: `150`.
