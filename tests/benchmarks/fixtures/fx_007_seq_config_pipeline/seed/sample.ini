# Sample configuration file for testing and validation

[database]
host = db.local
port = 5432
user = postgres

[api]
port = 8080
url = http://${database.host}:${api.port}/v1
timeout = 30

[client]
base_url = ${api.url}
retries = 3
