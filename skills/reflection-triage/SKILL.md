Create a reflection-triage skill encoding the watchdog checklist as an ordered read-only runbook: (1) sqlite integrity check + last-run row inspection on var/omniagentos.db, (2) launchd stdout/stderr tail for the nightly job, (3) per-provider credential liveness ping on bootstrap (per the existing backfill lesson), (4) cap_exceeded_rate and harvester-count sanity checks, (5) explicit go/no-go recommendation on a manual re-run. No git or config mutations; diagnostics only.
