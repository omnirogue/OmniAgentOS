# PLAN3-TASK — R2 Reflection propose/apply/report (branch plan3/r2-propose-apply)
Spec of record: ~/Desktop/GrokOmni-Upgrade-Plans-20260726/PLAN-3-SELF-LEARNING-LOOP.md (read first) + docs/lessons/2026-07-26-plan1-execution-lessons.md.
Mission: Stages B, D, E — proposals from evidence, gated application with provenance, morning report.
OWN: omniagentos/reflection/propose.py, omniagentos/reflection/apply.py, omniagentos/reflection/report.py, omniagentos/api/routes/reflection.py (GET /api/reflection/proposals, GET /{id}, POST /{id}/approve|reject — register in api/main.py), dashboard/src/features/reflection/ + wiring the existing /improvements page to list+approve proposals (small surgical additions only), Makefile (add `reflect` and `reflect-apply` targets ONLY), tests/reflection/test_propose*.py test_apply*.py test_report*.py.
DO NOT create omniagentos/reflection/__init__.py (R1 owns the package init) and DO NOT write harvest/validate/watchdog modules. Code against the ImprovementProposal schema EXACTLY as the Desktop plan defines ({id, kind: model_config|formation|router_weight|effort_override|category_pin|brief_template|lesson|skill|rollback, target, current, proposed, rationale, evidence_refs[], predicted_impact, risk_class}); the integrator reconciles with R1's contracts.py.
BEHAVIOR:
- propose.py: build the analyst prompt from var/reflection/<date>/digest.md + evidence.json compiled_learnings (2-4 week lessons) + the hard-stop list; call the LLM via subprocess `command gemini --approval-mode=yolo -m gemini-3.6-flash -p <prompt>` capturing stdout (timeout 180s, retry once on parse failure); STRICT JSON extraction + schema validation; refused kinds/targets (hard-stop set: risk pins, configs/governance.yaml, omniagentos/policy/**, budget caps, credentials) are dropped WITH a note in the report; persist to reflection_proposals (status pending).
- apply.py: v1 auto-apply ONLY kinds lesson|brief_template (write docs/lessons/<date>-reflection.md entries) and model_config availability flags; all other kinds stay pending for human approval via the API/dashboard; approved config kinds applied by safe YAML edit (round-trip parse, minimal diff), committed to main-branch working tree ONLY when invoked by the runner on main with subject `reflection: <summary>` and author `reflection-loop <bot@omniagentos.local>` (mirror scripts/archi-morning/archi-morning.sh's commit pattern); every application writes a reflection_outcomes row; scoring pass reads yesterday's outcomes vs today's metrics and files kind=rollback proposals on regression.
- report.py: vault/briefings/reflection-<date>.md with sections: What was learned / What changed automatically / Needs your decision / Yesterday's changes scored / Loop health. Reuse vault writer patterns from omniagentos/vault/.
- Makefile: `reflect` = harvest+propose+report observe-only; `reflect-apply` = + validate+apply (call modules via uv run python -m omniagentos.reflection.runner if present else chain modules; keep targets thin).
RULES: work only here; never touch main; observe-only default (no config writes unless reflect-apply + approved); lazy imports; uv run pytest -q tests/reflection green + `cd dashboard && npm run build` clean before final commit (node_modules is symlinked — never commit it); commit conventionally per stage.
