# PLAN3-TASK — R1 Reflection harvest (branch plan3/r1-harvest)
Spec of record: ~/Desktop/GrokOmni-Upgrade-Plans-20260726/PLAN-3-SELF-LEARNING-LOOP.md (read first). Seed corpus with source maps: docs/lessons/2026-07-26-plan1-execution-lessons.md (read it — the Kimi harvest map is in there).
Mission: Stage A — mechanical evidence harvest for the nightly self-learning loop. No LLM calls in this lane.
OWN: omniagentos/reflection/__init__.py (YOU create the package — other lanes are forbidden), omniagentos/reflection/contracts.py (dataclasses + JSON schemas incl. ImprovementProposal exactly as the plan defines), omniagentos/reflection/harvest.py, omniagentos/reflection/adapters.py, omniagentos/reflection/taxonomy.py, configs/reflection.yaml (caps + windows + provider adapter toggles), omniagentos/db/migrations/076_reflection.sql (RESERVED 076: reflection_runs, reflection_proposals, reflection_outcomes — proposal lifecycle mirrors metacog PromotionStatus pending|shadow|promoted|rejected|superseded), tests/reflection/ (harvest+adapter tests with mock fixtures).
BEHAVIOR:
- Window: last 36 HOURS of transcripts/outcomes. Sources: sqlite (swarm_runs, swarm_attempts, sessions, formation_selections incl. predicted-vs-outcome deltas, metacog memory candidate counts — DB path resolution like the rest of omniagentos, honors OMNIAGENTOS_DB); ledgers var/runtime/ledger/sessions/*.jsonl (reuse omniagentos/sessions/manifest.py SessionManifest + the parsing approach in api/routes/sessions.py), var/modelintel/router_shadow.jsonl (incumbent-vs-challenger tallies), var/ledger/openhands_gate.jsonl (denials), var/runtime/ledger/runs-*.jsonl (extra.attempts[] per-attempt provider/model/effort/end_reason — richest failure data), var/swarm/<run>/<task>/WORKBOOK.md + TASK.md end-states.
- HARNESS ADAPTERS (each a SourceAdapter with discover() and extract(), all read-only, newest-first): claude (~/.claude*/projects/**/*.jsonl for every config_dir in configs/accounts.yaml), gemini (~/.gemini/tmp/** session logs), kimi (~/.kimi-code: session_index.jsonl -> sessions/<wd>/<session>/state.json (agents{} carries verbatim sub-agent briefs in swarmItem) -> agents/<id>/wire.jsonl -> agents/main/tasks/<id>/output.log; cheap metrics from logs/kimi-code.log 'llm response' lines; JOIN OmniAgentOS ledger rows to kimi sessions on time-window x workDir because ledger kimi rows have null model/usage), codex (~/.codex — discover layout, likely sessions/), grok (DISCOVER the grok CLI home — try ~/.grok, ~/.config/grok, ~/.grok-code; if absent ship the adapter returning empty with a loud discovery note in the digest).
- COMPOUNDING: also load docs/lessons/*.md and vault/briefings/reflection-*.md dated within the last 28 DAYS into a compiled_learnings section of the evidence (so each night reviews 2-4 weeks of accumulated lessons alongside 36h of raw transcripts).
- HARD CAPS (configs/reflection.yaml, enforced in code, asserted in tests): per-source raw-read byte cap (default 2MB), per-source digest cap (default 8000 tokens ~ 32KB), total digest cap (default 40000 tokens), oversized transcripts sampled head/tail + error-pattern grep (rate_limit, tool_error, wrong_model, timeout, retry, uncommitted, denied, hung, killed). Failure-taxonomy tagging is mechanical regex, kind list in taxonomy.py.
- OUTPUT: var/reflection/<YYYY-MM-DD>/evidence.json (typed, schema-validated) + digest.md (human-readable) + a reflection_runs row (started_at, finished_at, status, sources_read, bytes_read, caps_hit).
RULES: work only here; never touch main; no network; lazy imports so partial packages never break `import omniagentos`; uv run pytest -q tests/reflection green before final commit; commit conventionally per module.
