# PLAN3-TASK — R3 Reflection gates + watchdog + scheduler (branch plan3/r3-gates-watchdog)
Spec of record: ~/Desktop/GrokOmni-Upgrade-Plans-20260726/PLAN-3-SELF-LEARNING-LOOP.md (read first) + docs/lessons/2026-07-26-plan1-execution-lessons.md (lessons 16-17, 22-26 especially — they motivate this lane).
Mission: Stage C validation + the runner + THE FIXER FOR THE FIXER (watchdog) + scheduling.
OWN: omniagentos/reflection/validate.py, omniagentos/reflection/watchdog.py, omniagentos/reflection/runner.py (thin orchestration: harvest -> propose -> validate -> apply(observe-aware) -> report, each stage failure-isolated and logged to the reflection_runs row), scripts/reflection/ (reflect-nightly.sh, reflect-watchdog.sh, install-reflection.sh rendering TWO launchd plists: com.omniagentos.reflection-nightly at 02:30 and com.omniagentos.reflection-watchdog at 07:30 — RENDER ONLY, print the launchctl load lines, follow scripts/archi-morning/install-archi-morning.sh conventions exactly), tests/reflection/test_validate*.py test_watchdog*.py test_runner*.py.
DO NOT create omniagentos/reflection/__init__.py (R1 owns it); do not write harvest/propose/apply/report modules; lazy-import them inside functions so this lane tests standalone with mocks.
BEHAVIOR:
- validate.py: (1) static — schema validation, YAML round-trip on any proposed config edit, bounded-change budget per night (max 1 formation change, max 2 router-weight changes, configurable in configs/reflection.yaml — coordinate key names with R1's config, integrator reconciles), hard-stop refusal (risk pins incl. external/deploy/destructive->Claude, configs/governance.yaml, omniagentos/policy/**, budgets, credentials) — refusal is terminal and reported; (2) empirical hooks — router_weight proposals require challenger win-rate >= threshold over >= N decisions computed from var/modelintel/router_shadow.jsonl; orchestration-pattern proposals get a judge hook wrapping tests/policy/blind_judge.py as a callable (mock in tests; real invocation behind an env flag); bench canary hook stub invoking `make bench` with an env overlay (implement the interface + a TODO-marked minimal version if full wiring is heavy).
- watchdog.py — the fixer for the fixer: verifies (a) last night's reflection_runs row exists, status ok, finished within SLA; (b) var/reflection/<date>/evidence.json + digest.md exist, parse, and RESPECTED the caps (sizes within configs/reflection.yaml limits — an agent that blew its context budget is a failure); (c) briefing file written; (d) proposals rows schema-valid; (e) no reflection commit touched a hard-stop file (belt and braces: git log --since yesterday, author reflection-loop, diff paths checked). ON FAILURE: attempt exactly ONE re-run via runner.py; if that also fails, write an ALERT briefing to vault/briefings/reflection-ALERT-<date>.md AND file a board task titled 'reflection loop broken: <reason>' via the intake quick-dispatch store path (find how POST /api/intake/quick persists and call the store directly — no HTTP dependency), and exit nonzero so launchd logs it. The watchdog NEVER edits configs and never proposes improvements itself.
RULES: work only here; never touch main; never run launchctl; uv run pytest -q tests/reflection green before final commit; bash -n every script; commit conventionally per component.
