# PLAN2-TASK — Ops runtime install (branch plan2/ops-runtime-install)
Context: ALL old com.omniagentos.* launchd plists pointed at the deleted ~/OmniAgentOS and were archived today. This repo renders its own plists to var/launchd/rendered/ via scripts/*/install-*.sh. Supervisor: scripts/launch-omniagentos.sh (api 8485, dashboard 3001 — Docker currently squats 3001).
Mission: bring the aux runtime back under THIS repo's launchd, correctly.
OWN: scripts/** installer fixes only where needed, var/launchd/rendered/** (generated), docs/OPS-RUNTIME.md (new — the truth table: service -> plist label -> port -> how to restart).
Steps: (1) inventory scripts/*/install-*.sh: which render what; (2) fix any that still reference ~/OmniAgentOS paths; (3) render + `launchctl load` for: provider-sentinel, routines tick, swarm-optimizer, modelintel refresh, steward set, hygiene, curator, backlog-executor — honoring installers that deliberately refuse to auto-load (print the load command in the doc instead and say why); (4) dashboard: resolve the port — either free 3001 from Docker or standardize on 3002 (edit only the launcher env/plist, document the decision); (5) do NOT load com.omniagentos.archi-morning (owned by another lane today) and do NOT restart the API service (integrator owns it today) — document both as owned-elsewhere.
Acceptance: `launchctl list | grep omniagentos` shows the loaded set; docs/OPS-RUNTIME.md complete; every loaded job's log path exists under var/log/.
