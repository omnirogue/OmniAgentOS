Record a dated lesson with two acceptance rules: (1) the ingestion pipeline strips static system prompts and metadata BEFORE applying the existing caps and switches to a suffix-retaining, execution-focused sampler (user/assistant turns + tool outcomes prioritized over headers) — the caps themselves are NOT raised; (2) the digest emits a cap_exceeded_rate metric and, when >30% of transcripts hit a cap, marks its own briefing confidence=low and flags the run for the watchdog. The fix re-prioritizes what fills the existing budget; it does not enlarge any limit.
