You are Fable, chief architect, performing the R5 FINAL MERGE of the review round. Both reviewers audited the merged Chat v2 + Kanban (your R3 merge, in MAIN) and made targeted fixes in their own trees:
- /Users/youruser/OmniAgentOS-wtQ — Qwen: REVIEW-Q.md (2 bugs fixed: dock grow-a-chat panel refresh via onChatCreated/onChatMutated; plan-mode seedPlan job_id persisted to chat meta + new test; the rest noted-not-fixed)
- /Users/youruser/OmniAgentOS-wtK — Kimi: REVIEW-KIMI.md (21 fixes, all verified: scoped board empty state, classify-chip DTO refetch, message-merge poll race, composer stale closure, 11 missing CSS classes, composer-below-fold height, archived-chat list handling, Esc chain, back-button navigation, dock-copy fix on /chats, drawer inline-style removal)
- MAIN (your cwd) = /Users/youruser/OmniAgentOS — the R3-merged state both trees were forked from.

YOUR JOB: reconcile both review trees into MAIN. For each file the reviewers touched (diff each tree against MAIN in: features/chats, features/board, app/chats, app/board, app/activity, features/models), decide per hunk: take Q's version, K's version, or union them (they fixed DIFFERENT bugs in some of the SAME files — e.g. TaskDetailPanel.tsx, ChatSurface.tsx, useChats.ts; union is required there: Q's onChatCreated/onChatMutated + plan_job_id persistence AND K's fixes must both survive). Verify K's claimed fixes exist in its tree before trusting its report; verify Q's two fixes likewise. Do NOT take anything that reverts a SPEC.md contract.

RULES: only the 6 frontend dirs above are in play; never touch omniagentos/*, dashboard/src/design|lib, other features, docs. Preserve MAIN's concurrent-session drift. Write docs/ui-redesign/chat-v2/FINAL-MERGE-REPORT.md: per file — whose version/union + why, any fix you rejected and why, leftovers for the verification ladder. No preamble.
