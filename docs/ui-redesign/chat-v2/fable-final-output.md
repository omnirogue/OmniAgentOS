R5 final merge is complete. All 23 fixes from both review trees are reconciled into MAIN with nothing rejected, and `docs/ui-redesign/chat-v2/FINAL-MERGE-REPORT.md` records every decision.

**How the 20 files resolved:** 16 taken wholesale from Kimi, 2 from Qwen (`chatApi.ts`, `chatApi.test.ts`), and 2 true unions. In `ChatSurface.tsx`, Kimi's base (title refresh after first send, variant-aware empty-state copy, import cleanup) got Qwen's `seedPlan` → persist `plan_job_id` hunk added — the regions don't overlap. In `TaskDetailPanel.tsx`, Qwen's wiring won because it's a superset of Kimi's P2-8 (both `onChatCreated` and `onChatMutated` → `refresh()`), with Kimi's mandatory `task={task}` removal from the RunsTab call applied on top. Diffing the merged MAIN back against each tree confirms it differs from each by exactly the other reviewer's hunks.

**Verification of the reviewers:** every hunk in both trees maps to a claimed fix — except one undeclared Qwen file, `chatApi.ts`, which turned out to be a required supporting fix (fixture-mode `updateChat` ignored `meta` patches). I also verified Q2's whole premise chain in the backend: the store *merges* meta so the extra PATCH can't clobber, though the server already persists `plan_job_id` at POST /plan — so Q2 mattered mainly for fixture mode, which I noted in the report.

**No SPEC reverts.** The one deliberate deviation: Kimi's scoped-board disclosure says "see everything" instead of SPEC's example "see all N", because under server-side scoping the page only has the scoped count (N would render "all 0"). The e2e asserts only the title, which is unchanged. MAIN's concurrent drift (design/, lib/, omniagentos/, collab/, filters, models/, the ChatThread deletion) is untouched.

**One caveat:** this session's sandbox blocks node/npm entirely (subagent delegation hits the same wall), so the post-merge frontend ladder did not run. Both parent trees passed the full ladder independently, and the only untested combination — the two union files — I verified statically (props/types exist, no collisions). First thing on a terminal: `cd dashboard && npx tsc --noEmit && npm run lint && npx vitest run && npm run build` — expect 284/284 vitest (Qwen added one test).
