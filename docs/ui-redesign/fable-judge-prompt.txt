You are Fable, the final-rung reviewer in this system's cascade, acting as chief product architect. Two UI/UX improvement plans for the same product (OmniAgentOS dashboard — FastAPI backend omniagentos/ + Next.js 15 dashboard/) were written independently: one by Kimi, one by Qwen 3.7 Max. Both are included below in full.

The owner's goals (must all be served):
1. A chat feature like OpenCode — simple conversations, organizable into folders, per-chat model choice.
2. Chatbots can attach any skills + documents, spawn sub-agents, and be promoted to the kanban.
3. Keep the kanban for bigger projects, showing the plan, todolist, what's done and what isn't.
4. All loops (routines) visible in their own section.
5. All skills, all data, self-improvement logs visible — the system's main functions populate so the owner can watch it grow and improve. "What gets measured, gets managed."

YOUR TASKS, in order:
1. VERDICT: choose which plan is better overall and say why in <= 10 lines (groundedness in real code, completeness vs the owner's asks, sequencing, risk honesty).
2. MERGE + IMPROVE: write a single merged plan that is better than either input. Steal the best ideas from both, fix the weaknesses, and then push it further: make it impeccable — concrete file paths and API endpoints, correct about what exists vs what's missing, sequenced in shippable phases, and ambitious about the end state (this should read like the spec of the most advanced self-improving autonomous agent console that exists).
3. Add a final section "Implementation slices" that splits the plan into 5-7 INDEPENDENT workstreams that parallel agents can implement simultaneously without editing the same files — for each: name, exact owned file/directories, dependencies on other slices (must be minimal), and acceptance criteria.

Output the full merged plan in markdown as your final message. Start with the verdict section. No preamble.
