RESULT RECORDS: 14
================================================================================
## git-state
PURPOSE: Maps current OmniAgentOS state for V2 extension: tracks uncommitted WIP (filesearch route addition), ongoing fusion/overnight-integration branch (5 security/auth/proxy fixes staged), and recent main commits (auth fallback, fast-archive failed cards, live kanban board, files drawer, agentless executor) to identify collision points and reusable components before building V2 reliability system.
OVERLAPS_V2: **Approval + Escalation (Stage 3 partial)**: ApprovalGateway.resolve() already auto-approves safe classes, escalates money/delete/remote-destructive via NotificationEscalator → notifications DB; ready-to-reuse vocabulary (ActionClass, ApprovalRequest, ApprovalDecision). **Quality Gate (Stage 4 partial)**: Reviewer protocol + CrossLineageReviewer exist, codex-critic injected by default, CONFIRM/DENY + optional retries implemented. **Monitoring + Alerts (H12)**: StewardStore.create_alert() with severity/magnitude-aware cooldown, cooldown_key de-duplication, escalation thresholds already in place; open_alert_count() for dashboard. **Suggestions + Orchestration (H9)**: StewardStore.create_suggestion(), claim/release/decide state machine for approval panel; proposed_plan, risk_class, outcome tracking ready. **Live Board + Files (P2)**: Live kanban SSE, board_files API (upload/list/download/zi
TABLES: 
SCHEDULES: 
ENDPOINTS: 
EXTENSION POINTS:
  - omniagentos/steward/store.py + alerts table -> Add failure_detection_events table (failure type, rule_id, severity, evidence) and wire alert-monitor to fire on executor exception + step failure signals for 24/7 reliability audits
  - omniagentos/orchestrator/review.py + Reviewer protocol -> Extend CrossLineageReviewer.review() to accept 3-judge panel (codex-critic + opus-critic + grok) with voting/quorum; wire tie-breaker logic for split verdicts
  - omniagentos/orchestrator/core.py + Orchestrator.run() -> Add reliability audit stage (6a): post-completion inspect for degraded-mode runs, gate escalations, performance regressions; feed into steward goals + scorecard
  - dashboard/src/app/agents/page.tsx -> Wire agent org hierarchy (CTO/VPs/managers/specialists structure); display performance scorecards, escalation history, compliance trail per agent
  - omniagentos/api/routes/control.py + /api/pause -> Extend to /api/admin/approvals (pending list, bulk approve/reject + audit trail) and /api/admin/reliability-dashboard (failure detection, audit results, rollback points)
  - omniagentos/orchestrator/executor.py + runner_for_tier() -> Add rollback() method: revert to pre-execution state on reviewer DENY or critical failure; persist rollback_id + reason in runs table for audit
  - omniagentos/vault/ + note generation -> Add living-architecture-docs hook: wire architect agent to auto-update ADRs on schema/contract changes; detect staleness via ADR.frontier vs codebase HEAD
  - omniagentos/scheduler/routines_tick.py -> Add 'reliability audit' routine (twice-daily): invoke audit stage 6a, record verdict, update agent scorecards, feed failures into self-improve loop
GOTCHAS:
  ! FROZEN contracts: omniagentos/contracts.py + contracts/*.sql are locked (lead approval only) — V2 changes route through A2A questions; never edit directly
  ! Split-brain auth gate: 55d19c4 fix (55d19c4) reads request.scope['path'] not url.path — verify all new routes use the same; Starlette truncates url.path at '?'/#'
  ! fusion/overnight-integration branch (55d19c4) has 349 files changed (31k deletions, 814 insertions) — massive code cleanup; STILL UNMERGED as of 2026-07-23; collides on dashboard/, omniagentos/api/routes/*, omniagentos/orchestrator/* if not merged fi
  ! Multi-account auto-disable (32edd06): auto-disables dead Claude account after auth failure, respawns once on next enabled — ensure V2 audit/escalation doesn't re-trigger on same failure; verify account-rotation-seq increments correctly
  ! Approvals table: state enum (pending|approved|rejected|expired) — reviewers MUST update state via decide flow, not bypass it; expiry not enforced by runner yet
  ! StewardStore cooldown escalation: magnitude > 0 check (line 525) prevents re-alerting on $0 revenue days — gotcha for metrics that naturally go to zero (test RR-PROD-001)
  ! Orchestrator stage 4 (review): CrossLineageReviewer currently ONE reviewer (codex-critic) — multi-judge panel addition is new; merge consensus logic carefully
  ! Sessions hook-token: valid ONLY for /api/sessions/hook-eval; every OTHER mutating route requires full session token via main gate (line 64 _PUBLIC_MUTATION_PATHS allowlist is exhaustive, fail-closed)
  ! Dashboard board page: live-work guard (2515c58) prevents re-dispatching same task while in_progress — check guard logic if adding reliability state transitions
  ! Ledger + Vault persistence: runs serialized IMMEDIATELY on creation (manifest_path set) — audit trails MUST append, never modify existing ledger lines (immutable by design)
================================================================================
## skills-knowledge subsystems (skills library, Synapse knowledge graph, memory layer, vaultgraph)
PURPOSE: Skills library provides vault-backed, versioned instruction storage with update proposals and approval gates. Synapse (knowledge subsystem) stores facts in PostgreSQL+pgvector with role-separated promotion gates and 2-hop graph spreading. Memory layer assembles context from frozen conversations table + recalled facts. VaultGraph parses Obsidian vault into queryable graph for entity/concept search and fact classification.
OVERLAPS_V2: **Skill versioning + proposal workflow → "successful fix → reusable skill"**: Skills library already tracks versions + update_proposals with risk classification (low/model/major). V2 reliability audit can extend evidence_json to link audit run_id + validator scores, and route approvals through 3-judge panel instead of auto-approve. **Promotion gates (PostgreSQL role separation + PromotionGate capability object)**: ADR-005 established the pattern: the database is the boundary, not Python. V2 can reuse this — consolidator/admin role cannot be held by agents, making unsupervised promotion impossible. **Memory layer conversation tracking**: Frozen conversations table (migration 031) already captures project/task scope + turns + meta. V2 agent org hierarchy (CTO/VPs/managers) can store decision turns (role='system', meta.kind='decision') and trace approval chains. **Vault frontmatter contract
TABLES: skills (id PK, slug UNIQUE, category, subcategory, title, summary, preferred_method, fallback_method, vault_note_path, status, current_version, created_at, updated_at), skill_versions (id PK, skill_id, version UNIQUE(skill_id, version), content_snapshot, preferred_method, fallback_method, change_reason, evidence_json, author, status, created_at), update_proposals (id PK, skill_id, proposed_diff, p
SCHEDULES: com.omniagentos.curator — 2x daily (hour1, minute1, hour2, minute2 rendered in template); com.omniagentos.selfimprove-curator — 2x daily (orchestrator loop captures new skills); com.omniagentos.steward.* — daily/periodic briefing, metrics, comms, alerts; com.omniagentos.morning — daily morning report
ENDPOINTS: GET /api/skills/tree — list skills grouped by category/subcategory; GET /api/skills/search?q=<query>&limit=20 — search skills by slug/title/summary/methods; POST /api/skills — create skill (requires slug, category, subcategory, title); GET /api/skills/{id} — fetch skill with versions, current version, linked files; PUT /api/skills/{id} — edit skill content (creates new version); POST /api/skills/{id}/propose — propose update (risk=low|model|major, auto-approves low); POST /api/skills/{id}/restor
EXTENSION POINTS:
  - omniagentos/knowledge/store.py — PromotionGate usage -> V2 reliability audit: extend promote_fact() to accept audit_run_id + validator_score (3-judge panel outcome); gate callable ONLY from reliability service, not agent code
  - omniagentos/db/migrations/032_skill_library.sql -> Add reliability_validation table (skill_version_id, audit_run_id, validators_approved INT, improvement_type, next_version_id) to track which fixes landed from audits
  - skill_versions.evidence_json -> Expand evidence block to include {proposal_id, audit_run_id, validation_results: [{validator, score, notes}], rollback_point_if_failure}
  - omniagentos/vault/ note generation -> Create audit_report.md.j2 template: sections for failure summary, fix deployed, validation results, 3-judge panel decision, new skill entry (if promoted)
  - omniagentos/memory/ conversation tracking -> Track decision_made turns (meta.kind='decision', role='system') for agent org hierarchy — CTO/VP/manager approval trails
  - omniagentos/knowledge/contracts.py -> Add FailureEpisode (source='audit'), AuditResult (fact status 'promoted_from_audit'), ValidationRound (judge scores, decision timestamp)
  - launchd scheduling -> New com.omniagentos.reliability-audit label (twice-daily, staggered with curator) + com.omniagentos.admin-dashboard update (live agent org chart + approval queue)
  - omniagentos/vaultgraph/ classification -> classify_fact() extended to detect 'contradiction' edges → feed to 3-judge contradiction arbitration panel
GOTCHAS:
  ! FROZEN vault frontmatter contract (8-field YAML, contracts/vault-frontmatter.md): parse_frontmatter() fails loudly on extra/missing fields. Any V2 frontmatter extensions require contract amendment + council review (G1 criterion B7).
  ! Knowledge database grants are the REAL promotion boundary (ADR-005): Python PromotionGate is defense-in-depth only. Enforced by tests/knowledge/test_promotion_gate.py attacking both Python + real psycopg knowledge_agent role. Agent cannot UPDATE fact
  ! Skills library is vault-backed: vault/playbook/skill-*.md is the source of truth; index_vault_playbook() scans and upserts. Modifying skills table directly without updating the note breaks the contract.
  ! Conversations table present only post-migration 031 (July 2026): memory/store.py gracefully degrades to empty results if absent (pre-031 deployments). V2 must handle fallback.
  ! Agent role INSERT floor trigger (facts table, 001_init.sql): agents cannot insert active facts or high-trust/importance ones. All agent ingests land quarantined with trust ≤ 0.6, forcing consolidator review.
  ! Low-risk skill proposals auto-approve (propose_update with risk='low'): decide_proposal() immediately promotes the version, no manual gate. Model/major risks require explicit decide_proposal() call.
  ! Embedding dimension is frozen (EMBED_DIM = 1024 halfvec, bge-m3 only): mismatch detected at KnowledgeStore construction raises KnowledgeError. No runtime fallback; re-embed migration required if model changes.
  ! Dual-role PostgreSQL setup requires two DSNs: knowledge_agent (read-only credentials in ~/.pgpass) + knowledge_admin (in var/secrets/knowledge-pg.env, 0600, never agent-accessible). Mixing them breaks the security model.
  ! Recall budget (default 800 tokens) is HARD capped: render truncates whole facts to stay under budget_tokens * 3.5 chars. No graceful overflow — facts drop from end of recalled list.
  ! Skills preferred_method/fallback_method are text fields, not foreign keys: no enforcement that they reference valid methods. Operator must document what methods are valid in vault/playbook notes.
================================================================================
## Dashboard (Next.js mission control)
PURPOSE: Next.js 15 web UI for OmniAgentOS providing real-time mission control: live kanban task board with SSE updates, operator approval workflow, run/session monitoring, and global pause control. All mutations proxy through token-gated FastAPI backend via same-origin route handlers; browser never holds the session token. Design system with frozen tokens ensures coherent visual language across the app.
OVERLAPS_V2: The dashboard is the PRIMARY UI for OmniAgentOS V2. It already has: approval workflow skeleton (approvals page + decision dialog + SSE), session monitoring (sessions page with kill/message), health indicators (HealthBadge, PauseControl), and event-driven live updates (SSE with named event types). V2 will extend these: (1) Approvals page becomes a 3-judge panel view with confidence scores and audit trails; (2) New Judges section to show judge performance and decision reasoning; (3) New Reliability section for failure detection/recovery logs and incident tracking; (4) New Organization section to visualize agent hierarchy (CTO/VPs/managers); (5) Existing Sessions page extended with escalation chain and improvement suggestions; (6) Run detail pages gain audit trail of approvals + improvements. The design system and token-gated API proxy are load-bearing: all new V2 pages must stay within tok
TABLES: runs, tasks, approvals, steps, artifacts, disciplines, events (persisted events feed for SSE), receipts, sessions (for approval lookups, source=bridge or external), vault (galaxy graph nodes/edges - in vaultgraph.db separate from omniagentos.db), knowledge (entities in knowledge graph)
SCHEDULES: 
ENDPOINTS: GET /api/health; GET/PUT /api/pause (pause.changed SSE); GET /api/events (EventSource SSE, types filter, Last-Event-ID cursor); GET /api/runs, GET /api/runs/{id}; GET /api/tasks; GET /api/approvals?state=pending|approved|rejected, POST /api/approvals/{id}/decision; GET /api/sessions, GET /api/sessions/{id}, POST /api/sessions/{id}/kill, POST /api/sessions/{id}/message; POST /api/runs/{id}/cancel; GET /api/board (?archived=1), POST /api/board/{task_id}/archive; GET /api/projects, GET /api/project
EXTENSION POINTS:
  - Navigation: /Users/youruser/OmniAgentOS/dashboard/src/design/AppShell.tsx NAV_SECTIONS array -> Add new sections for V2 subsystems: Departments (agent org structure), Judges (approval panel dashboard), Reliability (failure detection/recovery logs), Audit (improvement memory)
  - Approvals page: /Users/youruser/OmniAgentOS/dashboard/src/app/approvals/page.tsx -> Extend to show judge reasoning, cross-model confidence scores, approval queue by risk level, auto-escalation rules
  - Sessions/Activity detail: /Users/youruser/OmniAgentOS/dashboard/src/app/activity/[taskId]/page.tsx and /runs/[id]/page.tsx -> Add audit trail (who decided what, when), improvement suggestions from this run, escalation history
  - Design system tokens: /Users/youruser/OmniAgentOS/dashboard/src/design/tokens.ts color definitions -> Add semantic colors for reliability states (healthy/degraded/critical/recovering), risk levels (low/medium/high/critical)
  - API proxy pattern: /Users/youruser/OmniAgentOS/dashboard/src/app/api/[...path]/route.ts -> New gated routes for V2 endpoints: /api/judges (panel decisions), /api/reliability (audit events), /api/agents (org tree read), /api/autonomy (toggle rules)
  - Events SSE types: /Users/youruser/OmniAgentOS/dashboard/src/lib/contracts.ts EVENT_TYPES -> Add: judge.decided, autonomy.toggled, reliability.incident, agent.cascaded, approval.escalated
  - Sidebar footer: /Users/youruser/OmniAgentOS/dashboard/src/design/AppShell.tsx lines 309-322 -> Add admin dashboard link for ops (logs viewer, pending approvals queue, reliability dashboard)
  - Page header eyebrow labels: various pages -> Establish 'Governance', 'Reliability', 'Organization' eyebrow categories for new V2 pages
GOTCHAS:
  ! API_BASE is '' (empty string) in contracts.ts - browser uses same-origin proxy, never direct loopback
  ! Session token at var/secrets/sessions-token must be 0600 (readable only by owner) - an agent confined by OS sandbox cannot read it, closing the control-plane escape vector
  ! Event cursor persisted in sessionStorage (omniagentos:lastEventId) - loses on tab close or private mode; resync event is synthesized if stream reconnects with a gap
  ! Last-Event-ID header (RFC 9110) is used for EventSource cursor recovery, not Last-Event-ID query param - dashboard maintains it in sessionStorage to survive reconnects
  ! SSE reconnect delay is 1.5s (RECONNECT_MS) for event stream failures - hardcoded, not configurable
  ! All frozen signatures in api.ts and useEvents.ts cannot change - p08 implements bodies, p09 codes against signatures concurrently (Wave 0 contract)
  ! Design tokens are TypeScript constants (color/space/font) AND CSS custom properties - theme.css applies them; features must use vars(), never raw hex
  ! AppShell navigation rebuilds on every pathname change - section open/close state is manual-override (undefined=auto, true/false=locked), avoiding thrash on route transitions
  ! Board 5s poll + SSE is intentional: SSE is real-time driver, 30s poll for sessions/approvals is a safety net when stream is down, not a primary refresh mechanism
  ! PauseControl reads pause.changed event payload.paused (boolean) - the pause endpoint is both gated read (GET /api/pause) and gated mutation (PUT /api/pause via proxy)
================================================================================
## db-schema
PURPOSE: SQLite persistence layer for OmniAgentOS: execution state (tasks/runs/steps), approvals/risk gates, self-improvement lab (experiments/surfaces/champions), agent collaboration, capabilities audits, budgets/revenue/banking facts, interactive sessions, projects with hierarchy and scoped permissions, recurring routines, skills library with versioning, and operational alerts/notifications. Migrated from frozen schema (001_init.sql) via 40 numbered migrations; current DB version 40.
OVERLAPS_V2: The existing schema already implements core V2 concepts: (1) **Risk gates**: approvals table with state/risk/decided_by/decided_at for action-class gating; (2) **Audit trail**: events table (SSE cursor-based) + capability_grant_log (append-only) + broker_calls log; (3) **Multi-account support**: claude_accounts table (auth_type, config_dir, email, enabled, is_default, last_used_seq for rotation); (4) **Session durable state**: sessions table (state, kill_requested, error, granted_roots for scope, todos_json for progress); (5) **Project hierarchy**: projects.parent_project_id + project_permission_grants for action-class overlays; (6) **Routines with gates**: routines (trigger_type, gate_type, hard_cap_type) + routine_runs (gate_passed/accepted tracking); (7) **Self-improvement**: surfaces/champions/experiments/eval_suites/judge_records/playbook/leaderboard (delayed_outcomes for auto-rollb
TABLES: 
SCHEDULES: omniagentos.scheduler.routines_tick — checks routines.trigger_type='cron' against routines.last_fired and current time; creates routine_runs + control-plane tasks on match; goal metric collector (hourly) — writes metric_snapshots and revenue_facts/bank_facts daily (ET calendar); reliability audits (twice daily, per V2 spec) — would read runs/approvals/events over sliding window, compute failure ra
ENDPOINTS: GET /api/store/tasks — list_tasks() DAL method, filtered/limited by state, project_id; GET /api/store/runs — list_runs() DAL method, filtered by state, project_id, limited by board window; GET /api/store/approvals — list_approvals() DAL method, filtered by state='pending', limited by oldest-first; GET /api/store/events — get_events_after(after_id, types) DAL method, SSE cursor-driven subscription feed; POST /api/approvals/{id}/decide — decide_approval() DAL method, updates state/decided_by/decis
EXTENSION POINTS:
  - runs table -> Add columns for failure_mode (e.g., 'timeout', 'auth_failure', 'rate_limit') and recovery_attempt_count to track retries; extend model to include failure detection logic in runner.py
  - approvals table -> Add approval_panel_votes table (id, approval_id, panelist_model, vote='approve'|'reject'|'escalate', reasoning_json, voted_at) to implement 3-judge cross-model panel; or denormalize decided_by as JSON
  - agents table -> Add role='specialist'|'manager'|'vp'|'cto', parent_agent_id for hierarchy, performance_score REAL for scorecards, and training_runs_count to track improvement
  - routines table -> Add reliability_audit_frequency='twice_daily'|'daily'|'weekly', audit_start_time TEXT (e.g., '06:00Z'), and auto_remediation_enabled INT; create reliability_audits table (id, routine_id, started_at, f
  - orchestrations table -> Add sandbox_state='setup'|'running'|'rollback_ready', rollback_checkpoint_path TEXT, and approval_required INT; track sandbox lifecycle separately from run lifecycle
  - sessions table -> Add rollback_point_path TEXT (snapshot of working_dir state), approval_panel_consensus REAL (confidence 0-1 of approvals.decision), risk_tier='low'|'medium'|'high' for autonomy level gating
  - new table -> Create admin_dashboard_pages (id, page_name='agents'|'logs'|'approvals'|'audit_results', layout_json, last_updated_at) to store dashboard configuration and cache queries for agent/run/approval/audit v
  - new table -> Create agent_org_hierarchy (child_agent_id, parent_agent_id, role_title, assigned_at, PRIMARY KEY(child_agent_id)) to enable manager/specialist role tree and delegation chains
GOTCHAS:
  ! **FROZEN schema (Wave 0)**: contracts/schema.sql is immutable; schema changes must go through lead via new migrations (comments in 001_init.sql and contracts/schema.sql state this explicitly)
  ! **Multi-process discipline**: Writer transactions use BEGIN IMMEDIATE to avoid deadlock; all code MUST keep write transactions short; WAL mode + busy_timeout=5000ms + NORMAL synchronous are set at connection time, NOT in DDL
  ! **Idempotency receipts in 006_broker_tokens.sql**: broker tokens are single-run and expire; reusing a token after expiry is a hard error (security); tokens are opaque (index run_id, not agent_id, to prevent inference of grants)
  ! **Held-out eval test answers protected**: eval_cases.expected_json is NULL for split='held_out'; actual expected values live ONLY in separate var/eval_protected.db (design review HD-001), opened by grader subprocess only (designs prevents reward-hack
  ! **Compare-and-swap on board_tasks**: claim_version is a CAS counter; exactly one agent wins a claim; all claim code MUST read-check-write atomically or lose to concurrent assigns
  ! **Project scoping F6 pattern**: list_runs_for_project / list_approvals_for_project / list_tasks_for_project / list_events_for_project apply WHERE filtering BEFORE ORDER BY LIMIT, not after, so a project's older work is never hidden behind a global wi
  ! **Event target_type and actor fields**: events.actor is 'api' | 'runner:<worker>' | 'scheduler' | 'user'; events.target_type + target_id + payload_json are the reference model (NOT run/approval/session pointers); approval events use target_type='appr
  ! **Timestamp format only**: All timestamps are UTC ISO-8601 'YYYY-MM-DDTHH:MM:SSZ' (contracts.utc_now_iso()); no Unix epoch or other formats; string comparison works because format is sortable
  ! **NO soft deletes**: Tables are append-only or updated in place; there is no 'deleted_at' column pattern; archival uses status='archived' or archived_at columns (e.g., board_tasks, skills)
  ! **Foreign key constraints ON**: PRAGMA foreign_keys=ON at connect time; orphaned rows cause constraint violations; deletion of projects cascades to project_permission_grants and project_scope_requests (ON DELETE CASCADE)
================================================================================
## Orchestrator-Runner (W0/W1 execution layer)
PURPOSE: The OmniAgentOS execution foundation: Fable orchestrator conducts multi-tier executor sessions (cheap→Fusion→Fusion-Ultra escalation), a durable runner state machine executes persisted step plans with checkpoints/retries/heartbeats, and the Session Bridge supervises long-lived Claude Code bridge sessions. All state lives in SQLite WAL; every run writes an JSONL manifest and vault note on terminal. Failure recovery via stale-worker reclaim, finalization quarantine, and auth-failure retry (1x per session). Auto-approve policy gates safety-class actions; escalates money/delete to operator notific
OVERLAPS_V2: Existing V0/W1 foundation already implements: (1) Fable-driven session orchestration with tier escalation (CHEAP/FUSION/FUSION_ULTRA) + cascade learning (win/loss traces); (2) Approve-safe + escalate-hard policy gates (auto-approve non-money/delete, escalate money/delete to operator notifications); (3) Cross-lineage reviewer (CrossLineageReviewer.review) for DENY feedback + Reflexion corrective retries; (4) Failure detection (LostFence, stale-worker reclaim, finalization quarantine, step retries + compensation, auth-failure 1x retry); (5) Durable state machines (RunState, TaskState, SessionState, ApprovalState) with checkpoints, heartbeats, and idempotency receipts; (6) Agent org hierarchy scaffolding (agent grants, discipline/task/run budgets, speaker roles in notifications); (7) Vault knowledge graph (vault notes per run/skill/decision, wiki auto-update on completion); (8) Admin dashbo
TABLES: schema_migrations — version, applied_at, disciplines — id (slug), name, metric_contract (JSON), status, created_at, tasks — id (tsk_*), discipline_id, title, input_json, state (TaskState), risk, deadline, created_at, updated_at, runs — id (run_*), task_id, discipline_id, agent, model, state (RunState), worker_id, plan_json (steps array), output_text, output_json, error, wall_ms, turns, input_token
SCHEDULES: launchd com.omniagentos.modelintel (07:15) — daily model-capability registry fetch + Grok web-search sweep; writes var/modelintel/registry.json + vault notes (models/, capabilities/, benchmarks/sources/); launchd com.omniagentos.routines (5-min tick via scripts/scheduler/com.omniagentos.routines.plist.template) — activity monitoring, automation triggers, self-improve curator; launchd com.omniagent
ENDPOINTS: POST /tasks — create task (API owned; projects/disciplines); GET /tasks/{id} — read task (state, latest run projection); PATCH /tasks/{id} — update task state (API owned transitions); POST /tasks/{id}/runs — enqueue run (plan=[agent step], API sets state=QUEUED; runner executes); GET /runs/{id} — read run (state, output, usage, manifest_path, vault_note_path); PATCH /runs/{id} — approve/reject/update run (cancel_requested flag only); POST /runs/{id}/cancel — set cancel_requested=1 (runner honors
EXTENSION POINTS:
  - omniagentos/runner/core.py::_notify_escalation -> Escalation notification seam (hard-stop approval, cap-hit, done, blocker) — currently calls omniagentos.sessions.notify.push; swap to multi-channel notifier (email, Slack, dashboard) for V2 operator p
  - omniagentos/orchestrator/approvals.py::classify_hard_stop -> Hard-stop signal detection (money/delete regex patterns) — extend with risk classification (low/medium/high/critical) to route escalations to 3-judge approval panel vs auto-approve
  - omniagentos/runner/core.py::_execute_step -> Step execution fault isolation — add failure detection hooks (LostFence, StepFailure, budget-exceeded, timeout) to emit reliability audit events and trigger auto-recovery logic
  - omniagentos/contracts.py::Events -> Add V2 event types: heartbeat.stale (worker unresponsive), approval.escalated (money/delete), run.failed (detailed failure_reason, recovery_action), session.auth_failure (retry=N), finalization_failed
  - omniagentos/runner/core.py::_record_finalize_failure -> Finalization quarantine (vault_note_path=_FINALIZE_QUARANTINE_SENTINEL after 5 attempts) — add human-readable recovery runbook to vault notes + notifications to escalate quarantine events
  - omniagentos/runner/core.py -> Concurrency state tracking (_in_flight set, _in_flight_lock) — expose per-slot health metrics (wall_ms, turns, cost_usd per slot, slot utilization %) to dashboard + reliability audit
  - omniagentos/sessions/supervisor.py::_process_finished -> Session failure recovery state machine — add attempt counters, backoff logic, and capability-degradation routing (e.g., if auth fails twice, try cheap-adapter instead of Fusion)
  - omniagentos/orchestrator/core.py::_execute_task -> Cascade tier escalation (CHEAP → FUSION → FUSION_ULTRA) already implemented; add 3-judge panel approval BEFORE reverting to cheap-tier after repeated failures (risk-classified autonomy)
GOTCHAS:
  ! FROZEN contracts: omniagentos/contracts.py is the cross-package source of truth (enums, Store Protocol, state machines, Ledger/Vault/API boundaries). Schema.sql and API/events.md are FROZEN; changes route through the lead. Design findings D-001…D-013
  ! Task projection rule: task state always reflects LATEST run (newest queued_at). Guard failures on projection (DB race) log as audit events, never raised. If updating task state, call Store.update_task_state(task_id, target, expect=[current]) with exp
  ! Fencing (D-006): every runner step-boundary write includes expect_worker=self. If expect_worker check fails (zero rows), the runner ABORTS immediately without continuing steps or adapter calls (another worker reclaimed it). Test this carefully; a sof
  ! Heartbeat pulse: upsert_heartbeat fires every loop iteration (not an event row per D-010) and in a daemon thread during step execution (interval = stale_s / 3). A missing heartbeat > stale_s (default 30s) makes a run stale for reclaim. Worker death d
  ! Finalization quarantine: runs that fail finalization 5+ times (default limit) are marked vault_note_path=_FINALIZE_QUARANTINE_SENTINEL without retrying. They are terminal but unfinalized; the operator must inspect and manually fix the underlying faul
  ! Session Bridge: PreToolUse hook runs BEFORE the tool (not after) and reads hook_token.py's per-session narrowly-scoped credential. The hook gates the tool call; if the gating fails, no approval row is created. Session transitions RUNNING → AWAITING_A
  ! Approval expiry: the runner samples the clock per parked run (not once per batch). If approval is past expires_at during service, it marks it EXPIRED and fails the run immediately. Expiry is deterministic: decision must be human-approved (gate.human_
  ! Cascade + Reflexion: if OMNIAGENTOS_CASCADE=1, every executor attempt (error or DENY) traces win/loss + optionally Reflexion-reflects failure evidence, then escalates one tier (CHEAP → FUSION → FUSION_ULTRA, capped). Reflexion is OFF by default (OMNI
  ! Knowledge subsystem optional: OMNIAGENTOS_KNOWLEDGE=1 enables Postgres+pgvector recall/ingest (migration-031+ required). If disabled or schema absent, every recall call is a no-op (never fails the run). Recall happens ONCE per run (marked in _recall_
  ! Memory layer optional: OMNIAGENTOS_MEMORY=1 enables conversation history injection + user/agent turn persistence. Pre-migration-031 or disabled = no-op. Memory state is per-run (_memory_state dict); a long-lived worker leaks entries if not cleaned up
================================================================================
## ledger-audit-policy: Immutable governance, execution replay, and approval-gating
PURPOSE: Records all run manifests in JSONL format; provides immutable audit events; classifies actions into 6 risk tiers (read_only → irreversible); gates hard-stop actions (file deletion, money moves) on human approval; enforces budget caps; persists idempotency receipts for recovery. Central cross-subsystem contract: ActionClass enum + is_hard_stop() predicate + approval_satisfies_gate() validator.
OVERLAPS_V2: 
Implements: (1) Immutable governance via ActionClass + is_hard_stop() predicate, frozen across packages. (2) JSONL run manifests in ledger/ for execution replay + auditing. (3) Approval state machine (PENDING→APPROVED|REJECTED|EXPIRED) with human-only gates (always_human predicate). (4) Audit event schema (events table, Events.AUDIT type, actor+action+target+payload). (5) Risk classification (6-tier ActionClass enum). (6) Budget enforcement (wall_ms, tokens, cost, turns caps). (7) Idempotency receipt tracking (idempotency table) for recovery. (8) Secret-read hard-stop (single registry in policy/secrets.py). (9) Session state machine (RUNNING→AWAITING_APPROVAL→RESUMING). (10) Approval expiry (expires_at vs now_iso, signed off by approval_satisfies_gate). Gaps for V2: (a) Single-human gate only; no 3-judge panel. (b) No failure detection/recovery agent. (c) No twice-daily reliability audi
TABLES: runs: action_class workflow state, worker claim, manifest_path (ledger location), vault_note_path, steps: seq, action_class (pre-execution classification), checkpoint_json (state before step), result_json, idempotency_key, approvals: action_class, state (PENDING→APPROVED|REJECTED|EXPIRED), decided_by, expires_at, run_id|task_id (dual mode: run-level + step-level via step_seq), events: type (audit.
SCHEDULES: launchd label: com.omniagentos.runner (scripts/launch-omniagentos.sh) — polling loop, 30s stale-worker reclaim window. Loaded via launchctl; env vars set in plist (OMNIAGENTOS_DB, OMNIAGENTOS_LEDGER_DIR, etc.).; Approval expiry: approval.expires_at is set at creation (now + approval_expiry_hours from policy.yaml, default 24h). Runner checks at step execute time; Hook checks on resume. No backgroun
ENDPOINTS: GET /api/runs: list runs (filters: state, task_id, limit); GET /api/runs/{id}: retrieve run + manifest + receipts + artifacts; GET /api/runs/{id}/steps: ordered step list (status, action_class, checkpoint, result); GET /api/approvals: list approvals (filters: state=pending|approved|rejected, limit); GET /api/approvals/{id}: single approval row; POST /api/approvals/{id}/decide: {state: 'approved'|'rejected', decided_by: str, note?: str}; GET /api/events: SSE stream (types: run.updated, approval.r
EXTENSION POINTS:
  - ActionClass tier ranking (policy/__init__.py _ACTION_CLASS_RANK dict) -> V2: Add new risk tiers for distinguishing deployment-class (reversible on prod) from data-class (irreversible on prod). Currently 6 levels; could become 8-10 with per-environment risk. Caller site: ru
  - Approval gate logic (policy/approval_satisfies_gate) -> V2: Wire 3-judge cross-model-family panel here. Currently: single human gate (_is_automation_identity predicate). Replace with: approval.decided_by becomes approval.judges (array of {model_family, ver
  - Runtime policy config (configs/policy.yaml) -> V2: Add approval_panel config (judges, quorum rules, retry logic). Add risk escalation matrix (e.g., discipline=payments + ActionClass=IRREVERSIBLE → always_human + quorum=3). Add audit-event whitelis
  - RunManifest finalization (runner/core.py finalize_run) -> V2: Record failure detection + recovery attempt log. Add repair_action field (e.g., {attempted: 'retry', succeeded: bool, steps_rolled_back: []}). Extend receipts to include idempotency recovery statu
  - Session policy_map.classify_tool (sessions/policy_map.py) -> V2: Add agent-org context: if session spawned by manager-agent, auto-upgrade EXTERNAL_REVERSIBLE→CONSEQUENTIAL. Add capability-tier matching (agent: junior/senior determines tool allowlist enforcement
  - Audit event schema (contracts/Events.*) -> V2: Add performance_metric event type (agent wall_ms, output_quality score, budget efficiency). Add improvement_applied event type (logging when vault-learned patterns are deployed). Extend payload_js
  - Budget module (omniagentos/budget/__init__.py check) -> V2: Add per-discipline budget buckets (research-briefs gets 100M tokens/day, code-changes gets 50M). Add sliding-window enforcement (last-6-hour usage, not daily). Track budget-exhaustion backoff (whe
  - Secret registry (omniagentos/policy/secrets.py) -> V2: Support dynamic secret-path updates from vault (agent learn patterns: 'Stripe API lives in ~/keys/stripe.txt, not ~/.aws'). Add exception allowlist per-discipline (e.g., code-changes discipline ca
GOTCHAS:
  ! FROZEN CONTRACTS: ActionClass enum, is_hard_stop() predicate, RunManifest shape, Store Protocol, approval_satisfies_gate logic. Provisioning package imports is_hard_stop() to auto-grant scopes; changing HARD_STOP_CLASSES breaks auto-provisioning. Run
  ! AUTH QUIRK: is_hard_stop() imports ActionClass and HARD_STOP_CLASSES at module load; changing them at runtime (config reload) does NOT re-evaluate already-enqueued runs. Fix: require runner restart on policy.yaml change, OR wire a config-version hash
  ! WIP / INCOMPLETE: configs/policy.yaml has mode=supervised (legacy), but AUTO mode logic in policy.evaluate_action() shows future intent. AUTO mode only hard-stops IRREVERSIBLE (consequential + external_reversible auto-execute). Tests assume SUPERVISE
  ! SESSION BRIDGE DUAL-GATE: policy_map.classify_tool calls classify_shell for Bash, but supervisor hook also calls classify_shell independently. Both must agree. If shell.py is updated, supervisor must pick up the new version (no caching).
  ! APPROVAL EXPIRY TRAP: approval.expires_at is a deadline, but an APPROVED approval past expiry still must be checked (not usable for resume). approval_satisfies_gate checks (state in {PENDING, APPROVED}) AND expired → returns expired=True. Runner MUST
  ! LEDGER LEDGER-DIR RESOLUTION: default_ledger_dir() resolves from repo root or OMNIAGENTOS_LEDGER_DIR env. If runner and API are launched from different dirs with different env settings, they write to different ledger files (split-brain). Fix: runner/
  ! SECRET REGISTRY CASE-FOLDING: On macOS (case-insensitive filesystem), ~/.SSH and ~/.ssh are the same dir, but references_secret() case-folds to lowercase. A token '~/.SSH/id_rsa' will match ~/.ssh check. Linux (case-sensitive) does NOT lowercase; '~/
  ! DIR_GRANTS REALPATH TRAP: validate_grant_dir() resolves relative paths against realpath(allowed_grant_roots()). If OMNIAGENTOS_PROJECT_BASES contains a symlink, realpath collapses it; a write to the symlink path later may resolve to a different realp
  ! BUDGET TURN COUNTER: BudgetSpec.max_turns defaults to None (unlimited). Runner counts turns = loop iterations. If a run enters a spin loop calling the same tool, max_turns kicks in. But max_turns is NOT tracked in the DB (only wall_ms, tokens, cost),
  ! APPROVAL PANEL V2 MIGRATION: Currently approval row has single decided_by string. V2 will need approval.judges (JSON array) to store 3-judge verdicts. Migration path: (a) schema v2 adds judges column (nullable). (b) old rows have decided_by, new rows
================================================================================
## lab-experiments (H2 self-improvement subsystem)
PURPOSE: The self-learning empirical engine: runs blind-judged experiments comparing champion vs challenger surfaces (prompts, genomes, policies) against held-out eval cases, persists ELO-ranked tournament results, curates validated-trait playbooks, and two-times-daily promotes winning surfaces to production after multi-objective (quality/cost/complexity/safety) thresholds. Integrated with intake (task clarification), goals (metric collection), collab (agent coordination), agentless (patch generation), and promptshape (prompt segmentation).
OVERLAPS_V2: 
Directly implements H2 self-improvement (blueprint Section 11): experiment state machine, champion registry (reversibility + CAS), held-out eval protection (reward-hacking invariant HD-001), blind tournament ELO, playbook trait validation, complexity-regularized utility (HD-010). Benchmark/eval harness foundation: surfaces as mutable configuration (prompts, genomes, policies), eval_suites with protected held-out, EvalCase/EvalResult contracts. Orchestration genome (GenomeSpec) persisted as surfaces; tournaments score genomes; curator promotes winners. Intake system implements task entry: clarification loop + fast-lane classifier + board dispatcher for task → run conversion. Collab provides multi-agent substrate: board self-assignment (expertise CAS), messaging log, agent registry. Agentless (test-based patch selection) is a reusable skill. Self-improve tracks verified learnings to skill
TABLES: surfaces (id, kind: PROMPT|ORCHESTRATION_GENOME|MODEL_ASSIGNMENT|ROUTING_POLICY|REVIEW_RUBRIC, discipline, version, path, content_hash, status: draft|champion|challenger|archived, safety_relevant), champions (discipline, surface_kind PK, surface_id, cas_version [CAS guard], promoted_from_experiment, rollback_to_surface_id), champion_history (id, discipline, surface_kind, surface_id, event: promote
SCHEDULES: com.omniagentos.curator: launchd label for 2x-daily curation (07:00 + 19:00 ET by default; override OMNIAGENTOS_CURATOR_HOUR_1/2, MINUTE_1/2). Installed by scripts/curator/run.sh. Defense-in-depth: launchd unsets OMNIAGENTOS_EVAL_PROTECTED; curator env re-scrubs at call time.
ENDPOINTS: GET /api/lab/disciplines; GET /api/lab/experiments?limit=100&discipline=...; POST /api/lab/experiments {hypothesis,discipline,mutable_surface_kind,challenger_surface_id,eval_suite_id}; GET /api/lab/experiments/{experiment_id}; POST /api/lab/experiments/{experiment_id}/run {dry_run}; POST /api/lab/experiments/{experiment_id}/disposition {decision,note,decided_by}; GET /api/lab/surfaces?discipline/kind/status&limit=100; GET /api/lab/surfaces/{surface_id} {path,content_hash,version,status}; GET /ap
EXTENSION POINTS:
  - omniagentos.lab.tournament.core.execute_genome module -> Genome executor (L04x) injected at runtime; TournamentDriver falls back to omniagentos.lab.executor.execute_genome if available
  - omniagentos.lab.curator.agent.run_curation_agent(prompt, dry_run) -> Optional live Sonnet-high narrative pass on curation results; disabled by default (enable OMNIAGENTOS_CURATOR_LIVE_AGENT=1)
  - omniagentos.intake.service.default_clarify_llm(prompt, schema) -> Clarifying LLM: Fable medium via CLI by default; env: OMNIAGENTOS_INTAKE_MODEL/EFFORT; falls back to heuristic on unavailability
  - omniagentos.agentless.pipeline.ResolveFn callback -> Adapter resolution; defaults to omniagentos.adapters.registry.resolve_adapter, testable with stub implementations
  - omniagentos.routing.cascade.run_cascade(work, trace_path, config_path) -> Tier cascade: cheap→expensive on verified failure with Reflexion; tiers defined in configs/cascade.yaml
  - omniagentos.knowledge.consolidate.consolidate(kb_store, vault_dir) -> Knowledge consolidation (promotion, dedup, decay, adjudication) runs in curator if OMNIAGENTOS_KNOWLEDGE=1 + admin DSN set
GOTCHAS:
  ! FROZEN contract (HD-001/HD-ALT): eval_cases.expected for split='held_out' is NEVER stored in shared var/omniagentos.db—only in separate L02 protected store var/eval_protected.db. Reward-hacking invariant enforced structurally. CandidateEvalCase strip
  ! Champions use compare-and-swap (cas_version) guard; set_champion() must be atomic. ChampionCASMismatch on version mismatch (HD-006).
  ! safety_relevant surfaces (REVIEW_RUBRIC, ROUTING_POLICY) ALWAYS require human approval for promotion (design review HD-003). Forced by surface_requires_human().
  ! Curator NEVER sees L02 protected store path (HD-001): launchd plist unsets OMNIAGENTOS_EVAL_PROTECTED, omniagentos.lab.curator.env re-scrubs at call time.
  ! Tournament judge sees only blind_token ('candidate-a'/'candidate-b'), never config_id. _blind_payload() strips identity.
  ! Agentless applies=False if diff never applied, tests_passed=None if verify never ran. Do NOT conflate.
  ! Fast-lane classifier is pure heuristic (zero LLM). When unsure returns 'planned'—false planned costs planning time; false fast under-scopes.
  ! ClarifyResult.forced=True on round budget exhausted (MAX_CLARIFY_ROUNDS=2); agent MUST emit spec on final round.
  ! Board tasks use claim_version CAS: exactly one agent wins claim race.
  ! Orchestration DAL isolated SQLite (orchestrations.db); OperationalError silently degrades if table unavailable.
================================================================================
## selfimprove-steward
PURPOSE: Self-improvement curator scans recent COMPLETED runs, captures reusable skills + constraints via verification gates, and proposes/auto-approves low-risk skill updates. Steward monitors goals via Stripe/Meta metrics, detects failures (ROAS floor, spend spikes, payment failures, revenue crashes) with cooldown-aware alerting, generates deterministic goal-rule suggestions (read-only only at rung 1), composes daily briefings, and curates/extracts inbound comms for knowledge linking. Both systems persist audit trails (alerts, suggestions, briefings, metric snapshots) in SQLite and emit to Slack/emai
OVERLAPS_V2: The curator implements the core self-improving loop (run→skill→vault) with verification gates; V2 extends this with per-run improvement tracking, reflection storage for failed attempts, and skill performance feedback. The steward implements deterministic failure detection (ROAS, spend, revenue, payment rules) + cooldown-aware alerting + low-risk suggestion generation (rung 1 autonomy). V2 extends with multi-judge approval panels, risk-classified autonomy tiers, agent org hierarchy, performance scorecards, and living ARCHI docs agents can update. Both systems already emit to Slack/email/voice and track audit trails; V2 layers reliability heartbeats, recovery agents, twice-daily audits, and rollback points on top.
TABLES: goals, metric_snapshots, goal_facts, comms_messages, comms_sources, alerts, suggestions, briefings, tasks, runs, steps, approvals, events, artifacts
SCHEDULES: com.omniagentos.selfimprove-curator: 3:30 AM daily, 3:30 PM daily; com.omniagentos.steward.alerts: every 900 seconds (15 min); com.omniagentos.steward.briefing: 7:30 AM daily; com.omniagentos.steward.comms: 2:30 AM daily; com.omniagentos.steward.metrics: every 3600 seconds (hourly); com.omniagentos.cache-gc: daily (via launchd template)
ENDPOINTS: GET /api/alerts; GET /api/alerts/count; POST /api/alerts/{alert_id}/ack; GET /api/suggestions; POST /api/suggestions/generate; POST /api/suggestions/{suggestion_id}/approve; POST /api/suggestions/{suggestion_id}/dismiss
EXTENSION POINTS:
  - omniagentos.steward.alerts.monitor.monitor_once() -> Add new rule function() -> list[AlertCandidate] for custom detection; signature matches roas_floor, spend_spike, payment_failure_burst, revenue_drop
  - omniagentos.api.routes.suggestions.py:approve() -> Extend before create_task_service() call to gate on multi-judge cross-model approval panel voting; store votes in new approval_panel_votes table
  - omniagentos.steward/store.py:goals table -> Add manager_id, cto_id, vp_discipline_id fields; new agent_assignments(agent_id, role, goal_id) table for org hierarchy
  - omniagentos.db/migrations/ -> New migration for agent_scorecards(agent_id, metric, window, value), agent_audit_log(agent_id, event, outcome, created_at)
  - omniagentos.api.routes/ -> New /api/admin/agents, /api/admin/hierarchy, /api/admin/logs endpoints; gate on policy (admin-only)
  - omniagentos.selfimprove.curator -> Hook into run completion to auto-update living /vault/playbook/ARCHI-<discipline>.md docs; track diffs as run artifacts
  - omniagentos.selfimprove.reflexion -> Extend to track per-agent success/failure patterns; query in cascade routing for reflection + agent selection
  - omniagentos.steward.config -> New RiskTierConfig section with tiers, thresholds, autonomy_rung_per_tier; gate suggestion.approve() by task.risk + tier
GOTCHAS:
  ! Wave 0 contracts/schema.sql is FROZEN; schema changes require lead approval and migration files (007_steward.sql pattern)
  ! omniagentos.steward.store (RA-owned) is frozen; no post-creation alert evidence update path — delivery outcome stored on audit event instead
  ! HARD RULE: capture_skill() and append_constraint() REFUSE unless gate.status is GateStatus.PASSED — never bypass via shortcut boolean
  ! reflexion.py JSONL store (reflections.jsonl) is intentionally separate from skills/constraints/vault; failure narratives must never mix with verified-only surfaces
  ! Autonomy Rung 1: no auto-approval identities allowed in suggestions.py:approve(); approved_by is human-only, case-insensitive check against RESERVED_AUTO_IDENTITIES
  ! Knowledge bridge (comms→extraction) is optional; extract_batch.py always exits 0 regardless
  ! Triaged message tracking: external steward-triaged.json file (not DB) prevents LLM re-triage in 15-min monitor cycles; path derived from default_db_path()
  ! com.omniagentos.steward.briefing loads OMNIAGENTOS_KNOWLEDGE env vars (postgres DSN); briefing fails if knowledge subsystem configured but unavailable
  ! Cooldown logic: escalation on magnitude jump (1.5x) OR severity upgrade; same severity + magnitude <= 1.5x prior = suppressed (RR-PROD-002)
  ! Curator runs at 3:30 AM + 3:30 PM; curation is NOT real-time (no hook inside runner); run ledger is append-only and read in scan order
================================================================================
## routing-models (Subsystem Group 1: Model Routing + ModelIntel Registry + Multi-Account Management + CLI Adapters)
PURPOSE: OmniAgentOS routing orchestrates model invocation across Anthropic, OpenAI, and xAI (plus 6 other providers). An LLM router using Grok evaluates tasks and selects optimal models; a mechanical router scores available coding agents; a verification cascade chains fallback attempts (Fable→Opus→Sol). The modelintel registry merges curated priors + live benchmark data into domain-scored model profiles. Multi-account management pools Claude config directories for rate-limit resilience and spawns sessions with DB-tracked account rotation. CLI adapters (claude, codex, grok, kimi, gemini, etc.) invoke l
OVERLAPS_V2: The existing routing layer already implements core V2 mechanics: (1) multi-model-family orchestration (Anthropic, OpenAI, xAI, Meta, Moonshot, Google, DeepSeek, MiniMax, Alibaba) via adapters + registry; (2) model selection logic (LLM router, mechanical scorer, fallback chain); (3) rate-limit resilience (in-process account pool with cooldown, DB-backed session account rotation); (4) credential isolation (CLAUDE_CONFIG_DIR per account, secret_ref vault storage); (5) parallel invocation infrastructure (ThreadPoolExecutor in runner, subprocess groups); (6) audit logging (judge_records, eval_results tables, launchd-scheduled diaries). V2's judge panel (3-judge cross-model approval) can reuse the evaluator.judge_blind() loop by injecting an adapter-aware JudgeFn; no new routing primitives needed, only orchestration layer (panel membership, timeout/failure aggregation, persist decision metadat
TABLES: claude_accounts (id, label, auth_type, config_dir, email, secret_ref, enabled, is_default, status, status_detail, last_used_at, last_used_seq), sessions (provider, model, pid, lifecycle state), session_spawn_queue (durable session launch requests), judge_records (per-judge, per-case scores and notes), eval_results (deterministic experiment results), matches (tournament pairwise outcomes), leaderbo
SCHEDULES: scripts/scheduler/launchd.py — morning daily update (configurable hour/minute via StartCalendarInterval); scripts/selfimprove/launchd.py — 2x/day reliability audits (hour1:minute1 and hour2:minute2 calendar intervals)
ENDPOINTS: https://api.x.ai/v1/chat/completions (Grok router via modelintel/router.py:195); https://api.x.ai/v1/responses (Grok research endpoint; xAI-compatible via modelintel/research.py:87); https://aider.chat/docs/leaderboards/ (Aider polyglot benchmark fetch); https://swe-bench-live.github.io/ (SWE-bench-Live JSONL); https://openrouter.ai/api/v1/models (OpenRouter model metadata + pricing); https://artificialanalysis.ai/models (Artificial Analysis coding index + latency)
EXTENSION POINTS:
  - configs/modelintel.yaml models[]:key/provider/lineage/fusion_agents -> Add new ModelSpec for next provider family; router automatically scores via benchmarks + priors
  - omniagentos/adapters/registry.py REGISTRY dict -> Register new adapter class (e.g., cli-anthropic-nova → NovaAdapter); create adapters/<provider>.py subclass of CliAdapter
  - omniagentos/routing/account_pool.py AccountPoolConfig.pools.<provider> -> Wire multi-account credential rotation for new provider (discover_glob + accounts list)
  - omniagentos/lab/eval/evaluator.py judge_fn parameter -> Inject custom JudgeFn(lineage, JudgeView) to wire cross-model-family judge panel; currently injected as offline deterministic, not adapter-driven
  - omniagentos/intake/fallback.py run_with_fallback chain -> Extend hardcoded [fable, opus, sol] chain to include new lineages; currently sequential, single-threaded (see blocking gaps #2)
  - omniagentos/routing/cascade.py run_cascade -> Caller supplies work() and verify() lambdas; escalate to multi-model verification by injecting adapter resolution into verify() lambda
  - omniagentos/accounts/service.py detect_config_dirs() -> Hook for custom account discovery (e.g., environment-specific config dirs, cloud-backed secrets); currently walks glob patterns + DB
GOTCHAS:
  ! Account pool state is process-local + thread-locked; in a multi-process runner (launchd daemons), account cooldown state does NOT persist across processes — fallback chain handles this by advancing to next model lineage
  ! Modelintel router queries generated Fusion JSON files (~/.claude/fusion/model-rankings.json, model-intel.json), NOT registry.json directly — daily update cycle must succeed before router picks live scores
  ! ProtectedEvaluator.judge_blind() is sequential (no parallel judge calls); adapter-level subprocess calls are parallel but orchestrator loops serially — V2 judge panel needs async or ThreadPoolExecutor wrapper around judge_fn invocations
  ! Fallback chain [Fable → Opus → Sol] is hardcoded in fallback.py; cascade.py is generic but currently only used for verification, not model selection — next model family insertion requires code edit
  ! Grok is dual-use: both the router LLM (modelintel.router) AND a routable model (GrokAdapter); router calls xAI API directly (not via GrokAdapter), so router availability != model availability
  ! Database account table is for session launches (supervisor), separate from in-process adapter account pool — rate-limit state does not cross the boundary
  ! Adapters do NOT make direct HTTP calls; they invoke local CLIs (claude / codex / grok binaries) via subprocess — new providers must have a local CLI binary (no pure-Python OpenAI/xAI client adapters yet)
  ! OS sandbox wrapping (AC-policy #B1) is applied to all adapter calls; extra_write_roots passed via metadata grants must be pre-approved in runner.py — no runtime sandbox modifications
  ! Claude account pooling is opt-in (OMNIAGENTOS_ACCOUNT_POOL=1); disabled by default — scaling from one account requires explicit enable + config changes
================================================================================
## notify-comms
PURPOSE: Four integrated subsystems that reach the operator across all channels: (1) Notifications — durable feed of approvals, alerts, escalations with dashboard + push delivery; (2) Comms — inbound message ingestion from email (IMAP), Slack, Telegram with knowledge extraction; (3) Briefing — daily digest synthesizing 24h comms, metrics, approvals, alerts, delivered via email/Slack/voice artifact; (4) Voice — TTS synthesis to MP3 via ElevenLabs or xAI, playable through dashboard.
OVERLAPS_V2: This subsystem-group directly implements V2's **notification + outreach layer**: (1) **Failure detection/recovery agent** — approvals notify when runs fail/need decisions; alerts notify on rule violations; notifications table is the durable record and dashboard feed. (2) **Twice-daily reliability audits** — alerts.monitor runs every 900s, metrics.collect hourly, briefing daily 07:30; each writes to notifications + events log. (3) **Cross-model approval panel** — notifications fuel the dashboard approval queue (each approval_id links via ref_type+ref_id). (4) **Dashboard showing agents+logs+pending** — /api/notifications, /api/briefings, /api/comms/messages, /api/voice provide all signal. (5) **Living architecture docs** — vault briefing notes (briefings/YYYY/MM/DD.md) are human-readable summaries; can extend to auto-generate arch updates. Gaps for full V2: iMessage/SMS transport (only pu
TABLES: notifications (id, kind, title, body, severity, ref_type, ref_id, created_at, read_at, acted_at, payload_json; indexes: created_at DESC, read_at, ref_type+ref_id), comms_messages (id, source, external_id, thread_id, sender, recipients_json, sent_at, subject, body_text, raw_json, attachments_json, kb_status, episode_id, created_at; unique: source+external_id), comms_sources (name, kind, status, con
SCHEDULES: com.omniagentos.steward.briefing: StartCalendarInterval hour=7 minute=30 → python -m omniagentos.briefing.run; com.omniagentos.steward.comms: StartCalendarInterval hour=2 minute=30 → python -m omniagentos.comms.extract_batch; com.omniagentos.steward.alerts: StartInterval 900 seconds → python -m omniagentos.steward.alerts.monitor --once; com.omniagentos.steward.metrics: StartInterval 3600 seconds →
ENDPOINTS: GET /api/notifications; GET /api/notifications/count; POST /api/notifications/{notification_id}/read; POST /api/briefings/generate; GET /api/briefings/latest; GET /api/briefings; POST /api/briefings/{briefing_id}/ack; POST /api/comms/inbound; GET /api/comms/messages; GET /api/comms/messages/{message_id}; GET /api/comms/sources; GET /api/voice/providers; POST /api/voice/speak; GET /api/voice/audio/{artifact_id}
EXTENSION POINTS:
  - omniagentos/notifications/service.py — _push function -> To add iMessage: replace push() call with a platform-specific push provider (currently calls omniagentos.sessions.notify.push which is best-effort OS notification)
  - omniagentos/briefing/run.py — _deliver function -> To add SMS/iMessage delivery: add elif cfg.briefing.deliver_sms/deliver_imessage blocks alongside email/slack/voice
  - omniagentos/comms/pollers/ directory -> To add new message source (e.g., WhatsApp, Discord): create pollers/whatsapp.py with poll_once(steward, name) → dict, normalize output to comms_messages schema
  - omniagentos/voice/providers.py — TTSProvider contract -> To add new TTS provider (e.g., Google Cloud TTS, Azure): subclass TTSProvider, implement status() and synthesize(text, voice_id), register in omniagentos/voice/service.py get_provider()
  - omniagentos/briefing/gather.py — _comms function -> To customize briefing highlights: modify vip_senders list in StewardConfig.alerts, adjust top-10 ranking logic, extend GatherResult dataclass fields
  - omniagentos/comms/curate.py — select_for_extraction logic -> To change knowledge extraction filtering: modify select_for_extraction rules (currently filters by goal keywords, sender VIP status, subject match)
  - omniagentos/notifications/service.py — resolve_target function -> To add approval-target enrichment (e.g., show cost/risk inline): extend resolve_target to add computed fields from approval context
  - configs/steward.yaml — briefing section -> To add digest modes (e.g., 3-hour briefings, digest-vs-immediate): extend config schema with interval/frequency options, plumb through run_briefing CLI
GOTCHAS:
  ! Notifications table has frozen schema (migration 030_notifications.sql); kind enum is sealed at 6 values (approval, alert, escalation, done, blocked, info) — adding a new kind requires migration.
  ! Comms extraction has no terminal failure state — kb_status='failed' messages re-enter batch on next run (by design per M1 comment), so a malformed body will keep retrying; no per-message attempt counter exists.
  ! Briefing compose uses LLM (Sonnet) to narrate but rejects any number the LLM invents that isn't in gathered data (security boundary); if adapter fails or rejects narrative, falls back to deterministic-only.
  ! Push delivery via omniagentos.sessions.notify.push is best-effort and swallowed on failure — never breaks the caller. Actual implementation (ntfy vs OS notification) is delegated to sessions module.
  ! Comms pollers (Telegram, Slack, IMAP) track cursors in comms_sources.config_json — losing that row or corrupting the offset/ts can cause message duplication or miss.
  ! Knowledge extraction quotes untrusted sender/subject/body with quote_untrusted() before LLM sees it (SEC-O-005); raw comms_messages row stores untrusted text safely in body_text.
  ! Voice synthesis text is capped at 5000 chars (TEXT_CAP); longer briefing text is silently truncated before TTS.
  ! Briefing delivery happens AFTER persistence (H2 order: compute → upsert → write note → deliver); a delivery failure doesn't rollback the row, only records outcome in deliveries_json.
  ! Scheduled jobs all run as launchd agents in user context (~/.venv/bin/python); requires HOME env var and ~/.config/omni/connections.env for DB/knowledge credentials.
  ! comms_messages.external_id + source must be unique; webhook/poller idempotency relies on this constraint; duplicate payload = INSERT OR IGNORE = silent skip.
================================================================================
## Repomap + Filesearch + Living Architecture Docs
PURPOSE: Compact, task-aware code map (repomap) via PageRank over dependency graphs, federated file search (filesearch) across local+cloud via Spotlight+embeddings, and a git-versioned living architecture document (ARCHI.md in vault) that agents can update. Together they provide code context + file discovery + self-documenting architecture for V2 reliability agents.
OVERLAPS_V2: Repomap + filesearch + living-docs directly enables V2 core goals: (1) Failure detection/recovery: reliability agents call repo_map_for_task() + arch_context = load_arch_context() for code + subsystem boundaries before diagnosing failures (faster RCA, less hallucination). (2) Twice-daily audits: launchd jobs invoke reliability scan tasks. Filesearch finds relevant failure notes in vault/failures/. Repomap points to code sections changed since last audit. (3) 3-judge approval panel: judges review escalated tasks with instant code context from repomap + ARCHI.md; can call filesearch for similar past decisions in vault/decisions/. (4) Agent org hierarchy: living ARCHI.md documents reporting lines (CTO → VPs → managers → specialists); writes are git-versioned so org changes are auditable. Filesearch finds "who owns reliability subsystem" in vault notes. (5) Performance scorecards + admin das
TABLES: disciplines(id PK [slug], name, metric_contract JSON, status, created_at), tasks(id PK, discipline_id FK, title, input_json, state, risk, deadline, created_at, updated_at), runs(id PK, task_id FK, harness, model, state, plan_json, output_text, output_json, cost_usd, usage_estimated, budget_json, worker_id, queued_at, started_at, finished_at, created_at, updated_at), steps(id PK AUTOINCR, run_id FK
SCHEDULES: com.omniagentos.modelintel — 07:15 daily (scripts/scheduler/install-modelintel.sh); refreshes model registry + vault notes in vault/models/; morning-report — twice-daily (ADR-001); updates vault/Home.md stats block via vault.update_home(); filesearch reindex — manual invocation or optional launchd job; incremental (skips unchanged files by mtime)
ENDPOINTS: GET /api/health — system status + worker liveness; GET|PUT /api/pause — pause/resume execution; POST|GET /api/tasks — task CRUD; GET filters by state; POST /api/tasks/{id}/runs, GET /api/runs — run CRUD; GET filters by state/task_id/arm/harness; GET /api/events — SSE stream (run.updated, step.updated, approval.*, worker.heartbeat, audit.event, pause.changed); GET|POST /api/approvals/{id}/decision — approval routing; GET /api/ledger — JSONL manifest read (RunManifest rows, newest-first); GET /api
EXTENSION POINTS:
  - omniagentos/repomap/tags.py — add language extractor -> Add extract_go(), extract_rust(), etc. following extract_python()/extract_js_ts() pattern. Return same FileTags(rel_path, definitions, references) shape. Update iter_source_files() skip-dirs and _SOUR
  - omniagentos/filesearch/service.py — add search backend -> Add mount detector in curated_roots()/resolve_roots(). Register in search dispatcher. Must respect _INDEXABLE_EXT and placeholder-safety (metadata only, never trigger downloads).
  - vault/architecture/ — living ARCHI.md -> Create vault/architecture/ARCHI.md with subsystem H2 sections, entry points, DB tables, env vars, launchd labels. Link to ADRs/contracts. Use write_note() with autocommit=True. Preserve ## Notes (huma
  - omniagentos/orchestrator/ — add arch context to prompts -> Call both repo_map_for_task() and new load_arch_context(focus_terms, max_tokens=300). Parse vault/architecture/ARCHI.md, extract subsystem sections, prepend both maps to agent prompts.
  - contracts/ — V2 reliability APIs -> If V2 adds new DB tables, event types, or API routes, freeze in new contract files (e.g., contracts/reliability-api.md). Mark FROZEN + date + wave number.
  - omniagentos/vault/writer.py — concurrent write detection -> Extend write_note() return to (path, git_commit_hash). Add (run_id, updated_at) to frontmatter so agents detect concurrent writes and escalate to operator.
  - omniagentos/filesearch/catalog.py — model versioning -> Hardcoded 1024-d (ollama:bge-m3) is safe for H1-H3. Document H4+ model-switch migration: bump kb_meta.embed_model, re-embed all facts via consolidator backfill.
  - vault/playbook/ — living playbook updates -> H2 lab should update playbook notes as experiments conclude. Use write_note() + autocommit, preserving human sections (same pattern as vault/architecture/).
GOTCHAS:
  ! FROZEN contracts in Wave 0 are non-negotiable: api.md, events.md, interfaces.md, lab-api.md, lab-interfaces.md, schema.sql, statemachine.md, vault-frontmatter.md. Changes require explicit lead review + amendment (not config flag).
  ! Repomap ranking is task-aware but not filtering: calling build_repo_map() with focus_files/focus_terms biases ranking, not filters. A task naming 'intake' still sees all files ranked by centrality, with intake symbols boosted to top-8-per-file.
  ! Filesearch catalog is opt-in by path: curated_roots() only indexes named local folders (Documents, Desktop, Work, etc.) + mounted cloud drives. Missing a folder → not searchable. Add-your-own-path is future work (H2+).
  ! Embedding dimension fixed at 1024-d (ollama:bge-m3): changing models requires migration (bump kb_meta.embed_model, re-embed all facts). No runtime fallback or auto-detect; model mismatch RAISES error, not silently wrong.
  ! Vault autocommit is flag-gated (OMNIAGENTOS_VAULT_AUTOCOMMIT=1, default OFF in tests): agents writing ARCHI.md must set this flag, or writes stay uncommitted. Never auto-push.
  ! Concurrent ARCHI.md writes: if two agents update vault/architecture/ARCHI.md simultaneously, git sees a conflict on next write. No optimistic locking in write_note(); conflicts surface on disk, must be resolved manually or via 3-way merge. Recommenda
  ! ADR-005 knowledge subsystem: an agent with shell grant reading var/secrets/knowledge-pg.env (admin password) can bypass role separation. Documented as accepted same-uid residual until H4+ container gate.
  ! PageRank converges in 60 iterations (1e-8 tolerance): repos >1000 files with deep chains may not converge within 60 iterations. Increase iterations parameter if map quality is poor.
  ! Filesearch max 30k files default: OMNI_FILESEARCH_MAX_FILES env var controls this. Too high (100k+) slows indexing/query. First full index ~45 docs/sec. For large collections, filter via --scope at query time.
  ! ADR-007 autonomy: rung 1 has deterministic (metric-only) suggestions. Rung 1.5 (remote approval) and rung 2 (read-only auto-exec) are named prerequisites not yet built. Do not assume higher rungs exist.
================================================================================
## scheduler-launchd
PURPOSE: OmniAgentOS launchd scheduler system manages all background jobs (API, runner, Steward monitoring, briefing, metrics, alerts, comms extract, banking, revenue, model intelligence, cache GC, filesearch indexing, and self-improvement curation). Jobs are defined as plist templates, rendered by Python scripts, installed into ~/Library/LaunchAgents via shell installers, and executed by macOS launchd on fixed schedules (StartInterval in seconds or StartCalendarInterval at specific hours/minutes). The system separates safe-to-load jobs (keep-alive services, operator-approved recurring jobs) from budge
OVERLAPS_V2: The scheduler-launchd system already implements foundational V2 concepts: (1) scheduled background task execution via launchd with explicit time triggers (StartInterval/StartCalendarInterval); (2) a 5-minute routine-firing cycle that validates conditions before creating work (should_fire, validate_routine in routines.py) — precursor to V2 failure detection/recovery; (3) acceptance-rate tracking and auto-pause on floor breach (routines_tick.py, store.py) — proto-autonomy-level control; (4) split-decision pattern where safe jobs auto-load (install-steward.sh uses launchctl load) and risky jobs require manual review (install-routines.sh renders-only) — mirrors V2 approve-mode-first safety; (5) configuration externalization via YAML for audit trails (steward.yaml, policy.yaml); (6) crash-restart loops in scripts/launch-omniagentos.sh (supervise() function) that log and retry failures — light
TABLES: routines, routine_runs, events, goals, disciplines, tasks, runs
SCHEDULES: com.omniagentos.api (keep-alive); com.omniagentos.runner (keep-alive); com.omniagentos.routines (300s interval); com.omniagentos.steward.briefing (07:30 daily); com.omniagentos.steward.metrics (3600s interval); com.omniagentos.steward.alerts (900s interval); com.omniagentos.steward.comms (02:30 daily); com.omniagentos.banking (02:00 daily); com.omniagentos.banking.hourly (3600s); com.omniagentos.r
ENDPOINTS: POST/GET http://localhost:8484/task (create_task_service called by routines_tick); POST/GET http://localhost:8484/run (create_run_service called by routines_tick to enqueue work); GET/POST http://localhost:3000 (dashboard via npm run start/dev)
EXTENSION POINTS:
  - scripts/scheduler/install-*.sh -> Extend installer pattern (render template, optionally launchctl load) to add V2 twice-daily reliability audit jobs, failure-detection agent, and 3-judge approval monitor using selfimprove-curator.plis
  - omniagentos/scheduler/routines.py -> Hook V2 risk-classified autonomy levels into should_fire(): add risk_tier check, approval_mode flag, sandbox_required flag; return fire_decision with reasoning for audit log
  - omniagentos/scheduler/routines_tick.py -> Extend routine firing to emit reliability events (pre-fire checkpoint, post-fire success/fail, rollback trigger) that feed into V2 improvement memory and agent org scorecards
  - omniagentos/scheduler/store.py -> Add tables for approval_decisions (3-judge votes, timestamp, reasoning), sandbox_test_results (passed/failed runs), rollback_points (snapshots before risky jobs) for V2 auditing and recovery
  - scripts/reliability/launchd.py (new) -> Create new renderer accepting 2-4 time intervals and approval-mode flag to deploy V2 twice-daily reliability audit, failure-detection, and judge-panel jobs
  - configs/policy.yaml -> Extend to include approval_mode (always/never/risky), autonomy_tiers (full/supervised/sandbox-only), judge_panel_members (names/emails for cross-model approval); Steward jobs read policy and annotate 
  - omniagentos/api/services.py (task/run creation seam) -> V2 reliability checks (approval voting, sandbox test, rollback planning) should interpose before create_task_service/create_run_service, gating risky fires until approved
  - Admin dashboard pages -> Add tabs showing all launchd jobs, last fire times, approval status, reliability audit logs, and pending approvals; allow manual enable/disable/test-fire
GOTCHAS:
  ! No explicit launchd dependency keys (After, Requires) in plists: ordering encoded only in installer sequencing. V2 must adopt same pattern or clarify in docs.
  ! scripts/launch-omniagentos.sh has uncommitted WIP changes adding OMNIAGENTOS_CASCADE/REFLEXION/COMPRESS (ADR-009 Verified Compute). Do not commit without final review.
  ! Routines job deliberately NOT loaded by install-routines.sh: firing routines creates real tasks/runs and spends budget. Manual load after review is intentional.
  ! com.omniagentos.morning.plist exists but not loaded; com.omniagentos.steward.briefing at 07:30 is active equivalent. Coordinate if morning_report.py resurrected.
  ! All shell-wrapped jobs must source connections.env via 'set -a; . "$HOME/.config/omni/connections.env"; set +a' for credentials. Bare '. file' leaves them shell-local.
  ! Interpreter pinning: installers pin .venv/bin/python, fallback to python3.12. System Python 3.9 too old. Exit with error if missing.
  ! Knowledge subsystem exports (OMNIAGENTOS_KNOWLEDGE_PG_DSN, etc.) hardcoded in install-steward.sh and launch-omniagentos.sh. Update both if PostgreSQL DSN changes.
  ! Twice-daily jobs require StartCalendarInterval as array of dicts, not single dict. See selfimprove-curator.plist for working pattern.
  ! Crash-restart loops use 'if cmd; then ... else' not bare 'cmd; status=$?' because set -e exits loop. Do not simplify without reviewing errexit semantics.
  ! Log files default to /tmp or ~/OmniAgentOS/var/log/ and are not auto-rotated. Implement log-rotation to prevent disk fill.
================================================================================
## FastAPI Control Plane (omniagentos/api)
PURPOSE: Central API for OmniAgentOS, orchestrating tasks, runs, approvals, and events. FastAPI app binding 127.0.0.1:8484 with 25 route modules, SSE event streaming, session-token auth, pause/resume control, and approval state machine for safety-critical operations.
OVERLAPS_V2: API control plane is the ORCHESTRATION SURFACE for OmniAgentOS V2:

1. **Failure Detection/Recovery**: existing GET /api/health (worker heartbeats), events stream (error events). V2 adds: /api/recovery/{run_id}/request-restart, auto-pause logic via extended PUT /api/pause.

2. **Reliability Audits (2x daily)**: launchd infrastructure exists (com.omniagentos.* labels). V2 adds: audit route (/api/audit*), audit table, audit.audit event type, launchd job com.omniagentos.audit (0 AM + 12 PM).

3. **3-Judge Approval Panel**: approval subsystem exists (GET /api/approvals, POST /api/approvals/{id}/decision). V2 extends: Approval model gains judge_count, vote_results json. New: GET /api/approvals/{id}/votes, POST /api/approvals/{id}/vote.

4. **Risk-Classified Autonomy + Approve-Mode**: policy.Policy already checks scope. V2 adds: risk_level in tasks/runs, autonomy_level enum (auto|approve-neede
TABLES: heartbeats, pause, disciplines, tasks, runs, steps, events, artifacts, approvals, idempotency_receipts, budgets
SCHEDULES: com.omniagentos.morning (daily, default 8 AM): python -m omniagentos.scheduler.morning_report; com.omniagentos.banking (daily 2 AM ET): authoritative banking snapshot + hourly refresh (3600s interval); com.omniagentos.routines (every 300s): routine task execution cycle; com.omniagentos.modelintel (daily): model-intelligence training; Worker heartbeat polling: GET /api/health every ~15s (runner emb
ENDPOINTS: GET /api/health; GET /api/pause; PUT /api/pause; GET /api/disciplines; POST /api/disciplines; POST /api/tasks; GET /api/tasks; GET /api/tasks/{task_id}; POST /api/tasks/{task_id}/runs; GET /api/runs; GET /api/runs/{run_id}; POST /api/runs/{run_id}/cancel; GET /api/approvals; POST /api/approvals/{approval_id}/decision; GET /api/budgets; GET /api/events; GET /api/ledger; GET /api/projects/{id}/files/{file_path}; GET /api/mounts/*; GET /api/access/servers; POST /api/comms/inbound; POST /api/session
EXTENSION POINTS:
  - omniagentos/api/main.py:app.include_router() -> New route modules register here. V2 adds: admin.py (/api/admin*), recovery.py (/api/recovery*), org.py (/api/org*), archi.py (/api/archi*) for reliability + org hierarchy + admin dashboard.
  - omniagentos/api/routes/control.py:_emit(store, Events.*, type, ...) -> Event emission. V2 adds event types: reliability.audit, admin.approval_decision, org.agent_promoted. Emitted via _emit() → Store.insert_event.
  - omniagentos/api/main.py:require_session_token() dependency -> App-level auth gate. V2 extends with role/risk context for approval requirements + risk-classified autonomy. Use app.dependency_overrides in tests.
  - omniagentos/api/routes/control.py:@router.put('/pause') -> Pause control. V2 extends body: {paused, reason?, auto_unpause_after_ms?}. Track in pause table, schedule unpause event for runner.
  - omniagentos/api/routes/control.py:@router.post('/approvals/{approval_id}/decision') -> Approval decision. V2: multi-judge voting (judge_id → decision). Add GET /api/approvals/{id}/votes, POST /api/approvals/{id}/vote endpoints.
  - omniagentos/api/routes/control.py:async def events() SSE stream -> Event streaming. V2: filter by severity (error_only), add approval_panel event type, synthesize from approvals table (like session.updated).
  - omniagentos/api/routes/sessions.py:_hook_eval_authorized() -> Scoped token validation. V2: hook token carries role field (specialist|manager|vp|cto). Validate role before allowing tool.
  - omniagentos/api/models.py:class Approval -> Approval schema. V2: add judge_count, vote_results json, approval_type, created_by fields.
GOTCHAS:
  ! FROZEN Contract: contracts/api.md is Wave-0 locked. Endpoint signature changes require ADR + cross-team approval (breaking change to clients). Respect error envelope: {error: {code, message, detail}}.
  ! WIP main.py: filesearch router import + registration added but UNCOMMITTED. Verify git status before merging.
  ! Route Order: /api/projects/tree (hierarchy.py, order 9) must precede /api/projects/{id} (projects.py, order 10) or 'tree' matches {id} parameter.
  ! Session Token Gate: require_session_token() is app-level Depends(). Blocks ALL mutating routes. Exceptions: /api/comms/inbound (X-Comms-Token), /api/sessions/hook-eval (own token types). Project-file GETs, mounts GETs, /api/access/servers also gated 
  ! Pause Split: API only writes pause row + event. Runner owns queue logic. POST /api/tasks/{id}/runs allowed while paused; runner dequeues when unpaused.
  ! Approval Expiry (T-CODE-002): Session-linked approvals with expires_at <= now atomically expire during decide_approval(). Only session approvals; runner-approvals unchanged.
  ! SSE Replay: 500-event window before live events. If cursor + 500 < latest, client gets resync message (forces cursor reset). Long-disconnected clients may miss older events.
  ! Scoped Credentials: hook-eval accepts BOTH X-Session-Token (full) and X-Session-Hook-Token (scoped). Sandboxed bridge can't read var/secrets but can read own scoped token. Scoped token bound to exact session_id + route.
  ! Comms Inbound: /api/comms/inbound uses X-Comms-Token (per-source secret), NOT session token. External webhooks can't hold local token.
  ! ApiError Handler: ApiError (omniagentos.api.services.ApiError) caught at app level, rendered as {error: {code, message, detail}}. Other 422/404/500 errors have separate handlers.
================================================================================
## CRITIC
MISSED: Typed failure classification and recovery dispatch (transient/permanent/human-action classification, recovery-handler routing, dedicated failure events) @ omniagentos/runner/core.py, omniagentos/executor.py, omniagentos/orchestrator/ — exceptions are caught and logged as runs.error, but no classification
MISSED: Three-judge approval panel infrastructure (judge adapters for Fable/Sol/Terra, vote aggregation, quorum logic, approval-linked judge votes table) @ omniagentos/orchestrator/review.py invokes only CrossLineageReviewer; lab subsystem has blind judge support (judge_records) but not hooked to run appr
MISSED: Generalized rollback snapshots (state snapshots before arbitrary mutations, version restoration outside lab, rollback metadata, multi-version restore selection) @ omniagentos/lab subsystem has surfaces/champions/rollback logic (003_lab.sql, lab/api/routes/lab.py) but isolated to lab only; no run_snapshot, task_s
MISSED: Agent organization hierarchy (departments, manager/CTO/VP roles, manager_id relationships, org-tree structure, org-hierarchy API routes, dashboard org-chart visualization) @ agents table exists (004_collab.sql) with id/name/lineage/expertise/trust_level but no manager_id, department_id, org_role, or parent_agent_id columns
MISSED: Scoped autonomy levels (per-task, per-run, per-discipline, per-agent approve-vs-auto settings persisted in DB, resolution logic during execution) @ omniagentos/policy/__init__.py has global PolicyMode.AUTO|SUPERVISED and per-ActionClass policy; tasks table has no autonomy_level column; no per-scop
MISSED: Failure recovery and restart endpoints (POST /api/recovery/{run_id}/request-restart, failure detection/recovery agents, auto-pause logic) @ omniagentos/api/routes/ — no recovery routes found; PUT /api/pause exists but no recovery-driven restart logic
MISSED: Twice-daily reliability audit job and schema (reliability_audit_runs table, audit scoring/SLO calculation, launchd audit daemon) @ omniagentos/scheduler/launchd.py has com.omniagentos.routines and curator jobs but no audit-specific job; no audit table in schema
MISSED: Performance scorecards table and aggregation (per-agent/per-discipline metrics, rating endpoints, leaderboard) @ omniagentos/db schema — no scorecards table; budgets table exists but no scorecard roll-up or per-agent ranking
MISSING: Failure classification enum and recovery-dispatch layer
MISSING: Cross-model judge adapters (Fable/Sol/Terra) for approval panel
MISSING: Judge vote aggregation and quorum logic
MISSING: Run-linked judge metadata table
MISSING: Generalized snapshot/rollback outside lab
MISSING: Agent org hierarchy columns and relationships
MISSING: Org-tree API routes and dashboard visualization
MISSING: Per-scope autonomy_level persistence in DB
MISSING: Approval-mode resolution logic per task/discipline/agent
MISSING: Failure recovery endpoints and recovery agent
MISSING: Reliability audit launchd job
MISSING: Reliability audit table schema
MISSING: Performance scorecard table
MISSING: Scorecard aggregation and per-agent ranking
